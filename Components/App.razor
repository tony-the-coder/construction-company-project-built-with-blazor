<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />

    <link rel="icon" type="image/png" href="favicon.png" />

    @* Keep RadzenTheme component commented out - we load base CSS manually *@
    @* <RadzenTheme Theme="material" @rendermode="InteractiveServer" /> *@

    @* --- CORRECTED CSS ORDER --- *@

    @* 1. MANUALLY Load Radzen BASE CSS for component structure *@
    @*    This provides essential styles for Radzen components WITHOUT the full theme colors. *@
    @*    Choose ONE base theme below (material-base is common). Check path is correct. *@
    <link rel="stylesheet" href="_content/Radzen.Blazor/css/material-base.css" />

    @* 2. Load Compiled Tailwind CSS (handles overall theme) *@
    <link rel="stylesheet" href="styles/output.css" />

    @* 3. Load Blazor CSS isolation (loads last) *@
    <link rel="stylesheet" href="LehmanCustomConstruction.styles.css" />

    <HeadOutlet @rendermode="InteractiveServer" />
</head>

<body>
    <Routes @rendermode="InteractiveServer" />

    @* --- JS Includes --- *@
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
    <script src="_framework/blazor.web.js"></script>
    <script src="_content/Radzen.Blazor/Radzen.Blazor.js?v=@(typeof(Radzen.Colors).Assembly.GetName().Version)"></script>
</body>

</html>